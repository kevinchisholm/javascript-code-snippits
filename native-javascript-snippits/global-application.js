(function(_win, $){

    'use strict';

    /*global window: false */

    var utils = {};

    utils.init = function(){
        //initialize the applcation
    };

    utils.start = function(){
        //start the application
    };

    //initialize the applcation
    utils.init();
})(window, window.jQuery);